// src/modules/moduleNamed.js
// Note: file ends with .js but package.json has "type": "module", so this is an ESM file.

export function add(a, b) {
    return a + b;
  }
  
  export const description = "A tiny named-export module";
  